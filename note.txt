 SELECT 
     schemaname,
     tablename,
     policyname,
     cmd,
     roles::text[],
     qual,
     with_check
 FROM pg_policies 
     WHERE tablename = 'transactions'
     ORDER BY cmd, policyname;